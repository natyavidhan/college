Based on your query and the provided sources, here are the questions related to Linked Lists, Stacks, and Queues, extracted into separate tables.

### Table 1: Linked Lists Questions

| Question Paper ID | Question/Part/Subpart | Question Details | Sources |
| :---: | :---: | :--- | :--- |
| **1374** | 1.(a) | Give necessary class definitions to create a **circular linked list**. Write a member function to remove a node following the cursor node in the circular linked list. | |
| **1374** | 3.(b)(i) | Write functions for the following: Remove an element x from a **doubly linked list** of integers. | |
| **1374** | 3.(b)(ii) | Write functions for the following: Merge two **singly linked lists** of integers into one list. | |
| **1374** | 3.(i) | Give the output of the below function fun1 if 'fun1' is invoked as fun1(p) where p is a node pointer pointing to node 6 in the above **linked list** (6->4->3->1->2->7->NULL). | |
| **1374** | 3.(ii) | Give the output of the below function fun2 if 'fun2' is invoked as fun2(s) where s is a node pointer pointing to node 2 in the above **linked list** (1->2->3->4->5->6->NULL). | |
| **1527** | 1.(b) | What would be the output after the following code segment is executed on the following **linked list** (10 -> 30 -> 20 -> 90 -> 100)? | |
| **1527** | 2.(d) | Store the following univariate polynomial in a **singly linked list** (5x² − 6x³ − 3x + 2). | |
| **1527** | 2.(a) | What is the advantage of using a **doubly linked list** over a **singly linked list**? Write a function to remove duplicate elements from a sorted **doubly linked list**. | |
| **1527** | 6.(b) | Write a function to return the $i^{th}$ element of a **singly linked list**. Compare the time complexity of accessing an element in an array and a linked list. | |
| **1527** | 8.(b) | Write a function to delete the element pointed by the cursor in a **circular linked list**. | |
| **2046** | 1.(a) | Give template class definition for a **doubly linked list** of integers. Write a member function to insert a node at the end of this **linked list**. | |
| **2046** | 6.(i)-(iii) | Write functions for the following: (i) Creating an ordered **linked list** of integers. (ii) Merging two ordered **singly linked lists** of integers into one ordered list. (iii) Displaying the **linked list**. | |
| **32341301-2020** | 3.(b) | Trace the output of following function 'funcl' on the given **linked list** (2->4->6->8->5->1->12->11). | |
| **32341301-2021** | Q1 | Write a program to implement a ticket reservation system... Use **singly linked list** for storing information. Information is maintained in ascending order of names. Is there any disadvantage if information is not kept in a sorted way? | |
| **32341301-2021** | Q5(a) | For the NPPS scheduling algorithm, is a **linked list** suitable? | |
| **4343** | 1.(a) | An application requires frequent insertions and deletions to store and maintain its data items. Which data structure is appropriate: a **linked list** or an array? Justify... Justify the answer. | |
| **4343** | 1.(c) | Consider a scenario where every node of a **doubly linked list** stores a character... Write a function that visits each node of the **list** exactly once to check whether the contents of the **doubly linked list** form a palindrome or not. | |
| **4343** | 4.(b) | Assuming a class is already defined for a **singly linked list**, write a member function to display the middle node of the **list** by traversing the list exactly once. | |
| **4343** | 5.(a) | Give a class definition for an ordered **singly linked list**. Write a member function to insert a node in this **linked list** such that the list remains in order. | |
| **4343** | 6.(a) | Which searching technique... will this technique be appropriate if the given data is stored using a **linked list**? Why/Why not? | |
| **48** | 2.(a) | Write a recursive function to find and return the length of a **linked list**. | |
| **48** | 3.(a) | Write a program to merge two ordered **singly linked list** into a new ordered list. Give the necessary class definition with all required constructors. | |
| **48** | 4.(a) | Design a class for **doubly linked list** having integer data and write a function to delete all odd numbered nodes. | |
| **6504** | 1.(a) | Write enqueue and dequeue functions for a **queue** to be implemented through a **circular singly linked list**. | |
| **6504** | 2.(a) | Write a recursive function to display a **single linked list** of integers in reverse order. | |
| **6504** | 2.(b) | Write a member function to delete the element at $i^{th}$ position in a **doubly linked list**. The position i is passed as a parameter to this function. | |
| **7403** | 1.(c) | Given a **single linked list** of characters, write a function to check whether this list is a palindrome or not. | |
| **7403** | 2.(a) | Give template class definition for an ordered **singly linked list**. Write a member function to insert a node in this **linked list** such that the list remains in order. | |
| **7403** | 7.(a) | Given a **doubly linked list**, write an algorithm to swap the $k^{th}$ node from the beginning of this list with the $k^{th}$ node from the end of the same list. | |
| **Practicals List** | 1 | Write a program to implement **singly linked list** as an ADT that supports the following operations: Insert element x at beginning/i-th position; Remove element from beginning/i-th position; Search for element x and return its pointer. | |
| **Practicals List** | 2 | Write a program to implement **doubly linked list** as an ADT that supports the following operations: Insert element x at beginning/end; Remove element from beginning/end. | |
| **Practicals List** | 3 | Write a program to implement **circular linked list** as an ADT which supports the following operations: Insert element x in the list; Remove an element from the list; Search for an element x in the list and return its pointer. | |

***

### Table 2: Stack Questions

| Question Paper ID | Question/Part/Subpart | Question Details | Sources |
| :---: | :---: | :--- | :--- |
| **1374** | 1.(c) | Consider an electronic calculating device... Which data structure will the calculating device use in the algorithm? Show the steps of the algorithm used... and give the parenthesis free notation that the calculating device would have generated. | |
| **1527** | 3.(h) | Convert the following expression to postfix using a **stack**. Show the status of **stack** after each operation. | |
| **1527** | 5.(a) | Create a class to implement ADT **queue** using two **stacks**. Write functions - enqueue, dequeue, and empty. | |
| **2046** | 1.(d) | Evaluating the following postfix expression: BA + CD – x CB – AD – + /. Show the contents of the **stack** at every step. | |
| **2046** | 5.(b) | Write a C++ program to add two large integers using a **stack**. | |
| **4343** | 1.(c)(i) | Give the running time of Push() and Pop() operations for the above **stack** S (implemented using singly linked list). | |
| **4343** | 1.(b)(ii) | Can the efficiency of these operations [PUSH/POP] be improved using any alternate implementation strategy using a **singly Linked List**? | |
| **4343** | 7.(i) & 7.(ii) | Which data structures FastCal must use for the conversion and evaluation of the expression? Give the complete algorithm for the above-mentioned functioning of FastCal. | |
| **48** | 1.(a) | Evaluate the following postfix expression using **stack**, showing **stack** after each step. | |
| **6504** | 1.(c) | Evaluate the following postfix expression using a **stack**... Show the contents of the **stack** after every step. | |
| **6504** | 3.(a) | Write a function to reverse the order of elements in **stack** using two additional **stacks**. | |
| **7403** | 1.(a) | Convert the following infix expression to postfix form using a **stack**. Show the contents of the **stack** at every step. | |
| **Practicals List** | 4 | Implement **Stack** as an ADT and use it to evaluate a prefix/postfix expression. | |

***

### Table 3: Queues and Deques Questions

| Question Paper ID | Question/Part/Subpart | Question Details | Sources |
| :---: | :---: | :--- | :--- |
| **1374** | 1.(b) | Consider the following array-based **queue** of size 5... Show the contents of the **queue** with position of Front and Rear after each of the following operations done in sequence. | |
| **1374** | 5.(b) | Consider implementing **double ended queue (deque)** in three different ways using Array, **singly linked list**, and **doubly linked list**. Give running time complexity of all the below operations (insertFront(e), insertBack(e), deleteFront(), deleteRear()). | |
| **1527** | 1.(c) | For the given **queue** a, b, c, d, List the sequence of enqueue and dequeue operation to obtain the following queue: d, c, b, a. | |
| **1527** | 5.(a) | Create a class to implement ADT **queue** using two **stacks**. Write functions - enqueue, dequeue, and empty. | |
| **2046** | 1.(b) | Consider the following **Queue** of characters of size 6... This **Queue** is implemented as a circular array. Show the contents... after each of the following operations. | |
| **4343** | 1.(f) | Consider a circular **queue** created using an array of size 4. Perform the following operations... and show the status of the **queue** after every operation. | |
| **4343** | 1.(f) (Cont.) | If the above **queue** was linear, show the final contents of the **queue** after performing all the above operations. | |
| **48** | 2.(b) | A **queue** is represented in memory using a circular array of size 5... Represent Front and Rear in the **queue** after each step. | |
| **6504** | 1.(a) | Write enqueue and dequeue functions for a **queue** to be implemented through a circular singly linked list. | |
| **6504** | 4.(a) | Given a **queue** implemented using array of size 6. Show the **queue** and the front & rear values after performing each of the following operations. | |
| **7403** | 1.(b) | Consider the following **Queue** of characters of size 6... This **Queue** is implemented as a circular array. Show the contents... after each of the following operations. | |
| **7403** | 5.(a) | Given a **queue** of integers, write an algorithm that deletes all negative integers without changing the order of the remaining elements of the **queue**. | |
| **Practicals List** | 5 | Implement **Queue** as an ADT. | |

**Related Textbook Exercises/Concepts (Selected Examples):**

*   **Queue/Stack Relationship:** The sources contain questions regarding implementation: implementing a **queue** using two **stacks** and implementing a **stack** using two **queues**.
*   **Deque Implementation:** The implementation of a **deque** using two **stacks** is explored in the sources.
*   **Priority Queue Simulation:** Implementing a **first-in, first-out queue** using a priority queue is a conceptual problem found in the source material.
*   **Linked List Variants:** Several exercises detail the necessary procedures for working with different types of linked lists, such as: finding the middle node of a **doubly linked list**, reversing a **singly linked list** recursively or nonrecursively, and implementing **deque** operations using a **doubly linked list**.